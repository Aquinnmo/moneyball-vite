# Available Scripts

`npm run dev` - builds and runs a development version of the site. This will be hosted on localhost:5173

`npx eslint src/` - lints the entire `src` directory, append a filename contained in `src` to lint just that file